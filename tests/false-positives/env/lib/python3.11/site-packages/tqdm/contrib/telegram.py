"""Shape of tqdm.contrib.telegram: an exfil-looking endpoint that is the whole
point of the module, in code the user installed rather than wrote."""
API = "https://api.telegram.org/bot"


class TelegramIO:
    def __init__(self, token, chat_id):
        self.session = None
        self.url = API + token
